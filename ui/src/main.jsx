import './index.css'

import Plugin from './Plugin'
import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('plugin')).render(
  <React.StrictMode>
    <Plugin />
  </React.StrictMode>
)
